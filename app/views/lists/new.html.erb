<h1>Hi from new page 😎</h1>

<%= form_with model: @list do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>


<%= link_to 'Back to the main menu', lists_path(@lists) %>
